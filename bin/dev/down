#!/bin/bash

if ! type docker >/dev/null ; then
    echo "Docker not found!"
    exit 1
fi

export BASE_DIR=$PWD/.deploy

set -ex
docker compose --project-name elusivebot-dev --file docker-compose.dev.yml down
